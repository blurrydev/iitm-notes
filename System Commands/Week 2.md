# More Commands

- `more`:
- `head`:
- `tail`:
- `wc`:
- `which`:
- `whatis`:
- `apropos`: Same as `man -k {command name}`
- `alias`:
- `ls`:
- `stat`:
- `du`:
- 

# Arguments and Options

- Arguments:
- Options:

Sys
proc

# Shell Variables

